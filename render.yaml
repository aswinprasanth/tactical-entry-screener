services:
  - type: web
    name: tactical-entry-screener
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run app.py --server.port=$PORT --server.headless=true"
    autoDeploy: true
